import 'package:flutter/material.dart';

getFontsStyles(double size, Color color, {weight = FontWeight.normal}) {
  return TextStyle(fontSize: size, color: color, fontWeight: weight, fontFamily: 'Euclid Circular A');
}